pip_install_packages:
  - name: docker

redmine_image: redmine:5.1
redmine_port: 80

db_host: localhost
db_name: redmine
db_user: redmine
db_password: redmine

datadog_checks:
  http_check:
    init_config:

    instances:
      - name: redmine
        url: "http://localhost"
